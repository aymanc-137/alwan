<section >
    <div class="container">
        <div class="flex">
            {% for item in component.age_group %}
                <div class="flex-1">
                    <img src="{{ item.image }}" alt="">
                </div>
            {% endfor %}
        </div>
    </div>
</section>