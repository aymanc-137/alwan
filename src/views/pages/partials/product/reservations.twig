{#
| Variable     | Type  | Description |
|--------------|-------|-------------|
| reservations | array |             |                                                                                                      |                                                                          |
#}
<ul class="font-medium text-xs text-gray-600">
  {% for reservation in reservations %}
  <li class="py-1">
    <span class="text-unicode">
      {{ reservation.day|date|number }}
      {% if reservation.from_timestamp is not empty %}
        ({{ reservation.from|number({'PM': trans('common.time.pm'), 'AM': trans('common.time.am')}) }} - {{ reservation.to|number({'PM': trans('common.time.pm'), 'AM': trans('common.time.am')}) }})
      {% endif %}
    </span>
  </li>
  {% endfor %}
</ul>